<h1>ブログ一覧</h1>

<table>
  <tr>
    <th>内容</th>
  </tr>

<% @blogs.each do |picture| %>
  <tr>
    <td><%= picture.content %></td>
     <td><%= link_to '詳細を確認する', picture_path(picture.id) %></td>
  </tr>
<% end %>
</table>
<%= link_to '新しくブログを投稿する', new_blog_path %>
